setopt appendhistory # Append commands instead of overwriting
setopt sharehistory # Share history across sessions in realtime
setopt inc_append_history # Append the command in history as soon as its executed
setopt hist_ignore_space # Commands starting with <space> wont be stored in history(private commands)
setopt hist_save_no_dups # Same as hist_ignore_space but while writing to disc
setopt hist_ignore_all_dups
setopt hist_ignore_dups
setopt hist_find_no_dups
setopt interactive_comments # Allow comments in shell
setopt dot_glob
setopt extended_glob
setopt emacs
